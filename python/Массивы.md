**Примечание:** Python не имеет встроенной поддержки массивов, но вместо них можно использовать [[Список]] Python 
## Массивы

**Примечание:** На этой странице показано, как использовать списки в качестве массивов, однако для работы с массивами в Python вам потребуется импортировать библиотеку, например, [библиотеку NumPy](https://www.w3schools.com/python/numpy/default.asp)
Массивы используются для хранения нескольких значений в одной переменной:

### Пример
Создайте массив, содержащий названия автомобилей:

cars = ["Ford", "Volvo", "BMW"]

---

## Что такое массив?

Массив — это особая переменная, которая может хранить более одного значения одновременно.

Если у вас есть список элементов (например, список названий автомобилей), хранение названий автомобилей в отдельных переменных может выглядеть следующим образом:

car1 = "Ford"  
car2 = "Volvo"  
car3 = "BMW"

Однако что, если вам нужно пройтись по всем машинам и найти конкретную? А что, если у вас не 3 машины, а 300?

Решение — это массив!

Массив может хранить множество значений под одним именем, и доступ к этим значениям осуществляется по номеру индекса.

---

## Доступ к элементам массива

Для обращения к элементу массива используется _номер индекса_ .

### Пример

Получите значение первого элемента массива:

x = cars[0]

### Пример

Измените значение первого элемента массива:

cars[0] = "Toyota"

---

## Длина массива

Используйте этот `[len()](https://www.w3schools.com/python/ref_func_len.asp)`метод для возврата длины массива (количества элементов в массиве).

### Пример

Верните количество элементов в `cars` массиве:

x = len(cars)

**Примечание:** Длина массива всегда на единицу больше, чем наибольший индекс массива.

---

## Элементы массива в цикле

Цикл можно использовать `[for in](https://www.w3schools.com/python/python_for_loops.asp)`для перебора всех элементов массива.

### Пример

Выведите каждый элемент массива `cars`:

for x in cars:  
  print(x)

---

## Добавление элементов массива

Этот метод можно использовать `[append()](https://www.w3schools.com/python/ref_list_append.asp)`для добавления элемента в массив.

### Пример

Добавьте в `cars`массив еще один элемент:

cars.append("Honda")

---

## Удаление элементов массива

Этот метод можно использовать pop()для удаления элемента из массива.

### Пример

Удалите второй элемент массива `cars`:

cars.pop(1)

Этот метод также можно использовать remove() для удаления элемента из массива.

### Пример

Удалите элемент, имеющий значение "Volvo":

cars.remove("Volvo")

**Примечание:** Метод списка remove() удаляет только первое вхождение указанного значения.

---

## Методы работы с массивами

В Python есть набор встроенных методов, которые можно использовать со списками/массивами.

|Method|Description|
|---|---|
|[append()](https://www.w3schools.com/python/ref_list_append.asp)|Adds an element at the end of the list|
|[clear()](https://www.w3schools.com/python/ref_list_clear.asp)|Removes all the elements from the list|
|[copy()](https://www.w3schools.com/python/ref_list_copy.asp)|Returns a copy of the list|
|[count()](https://www.w3schools.com/python/ref_list_count.asp)|Returns the number of elements with the specified value|
|[extend()](https://www.w3schools.com/python/ref_list_extend.asp)|Add the elements of a list (or any iterable), to the end of the current list|
|[index()](https://www.w3schools.com/python/ref_list_index.asp)|Returns the index of the first element with the specified value|
|[insert()](https://www.w3schools.com/python/ref_list_insert.asp)|Adds an element at the specified position|
|[pop()](https://www.w3schools.com/python/ref_list_pop.asp)|Removes the element at the specified position|
|[remove()](https://www.w3schools.com/python/ref_list_remove.asp)|Removes the first item with the specified value|
|[reverse()](https://www.w3schools.com/python/ref_list_reverse.asp)|Reverses the order of the list|
|[sort()](https://www.w3schools.com/python/ref_list_sort.asp)|Sorts the list|

**Примечание:** Python не имеет встроенной поддержки массивов, но вместо них можно использовать списки Python.